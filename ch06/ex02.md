What is the type of `(,)`? When you use it in ghci, what does it do? What about `(,,)`?

---

The type of `(,)` is:
```haskell
(,) :: a -> b -> (a, b)
```

The type of `(,,)` is:
```haskell
(,,) :: a -> b -> c -> (a, b, c)
```
